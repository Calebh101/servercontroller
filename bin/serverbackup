#!/bin/bash
echo "Loading..."
echo "Getting directories..."

SCRIPT_DIR="$(dirname "$(realpath "$0")")"
PARENT_DIR="$(dirname "$SCRIPT_DIR")"

echo "Running called script..."
"$PARENT_DIR/backup.sh" "$@"